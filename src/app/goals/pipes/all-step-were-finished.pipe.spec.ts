import { AllStepWereFinishedPipe } from './all-step-were-finished.pipe';

describe('AllStepWereFinishedPipe', () => {
  it('create an instance', () => {
    const pipe = new AllStepWereFinishedPipe();
    expect(pipe).toBeTruthy();
  });
});
